<!DOCTYPE html>
<html lang="en" class="sg-flatwhite">
<head>
  <meta charset="UTF-8">
  <title>Styleguide</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="stylesheet" href="{{ rellink './stylegen-assets/vendor/prism.css' }}">
  <link rel="stylesheet" href="{{ rellink './stylegen-assets/styles/stylegen-flatwhite.css' }}">
</head>
<body>
  <header class="sgfw-header">
    <h1 class="sgfw-h1">Styleguide</h1>
  </header>

  <div class="sgfw-content-wrapper">
    <nav class="sgfw-nav test-stylegen-main-nav">
      {{#*inline "sg.main-link-item"}}
        <li class="sgfw-nav-item sgfw-list-item">
          <a class="sgfw-nav-link" href="{{ rellink link }}">{{label}}</a>
          {{#if children}}
            <ul class="sgfw-nav-children test-stylegen-children">
              {{> sg.main-link-list children}}
            </ul>
          {{/if}}
        </li>
      {{/inline}}

      <ul class="sgfw-nav-list sgfw-list">
        {{#*inline "sg.main-link-list"}}
          {{#each this}}
            {{> sg.main-link-item}}
          {{/each}}
        {{/inline}}

        {{> sg.main-link-list pages }}
      </ul>
    </nav>

    <main class="sgfw-content">
      {{{content}}}
    </main>
  </div>

  <script>window.jQuery || document.write('<script src=\'{{ rellink "./stylegen-assets/vendor/jquery.js" }}\'><\/script>')</script>
  <script src="{{ rellink './stylegen-assets/vendor/prism.js' }}"></script>
  <script src="{{ rellink './stylegen-assets/scripts/stylegen-flatwhite.js' }}"></script>
</body>
</html>
