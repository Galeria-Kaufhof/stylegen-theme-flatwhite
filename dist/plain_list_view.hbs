<div class="components">
  {{#if preflight}}
    <header class="sgfw-preflight-text test-stylegen-preflight">
      <h1 class="sgfw-h1">{{label}}</h1>
      {{{preflight}}}
    </header>
  {{/if}}

  {{#each components}}
    {{{compiled}}}
  {{/each}}
</div>
